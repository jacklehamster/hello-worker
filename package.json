{
  "name": "promo-codes-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts --config wrangler.dev.toml",
    "deploy": "wrangler deploy",
    "build": "bun i && bun build src/browser/* --target browser --outdir public --minify --sourcemap && tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  },
  "dependencies": {}
}
